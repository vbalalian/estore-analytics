type: dagster_project.components.custom_omni.CustomOmniComponent

attributes:
  workspace:
    base_url: "{{ env.OMNI_BASE_URL }}"
    api_key: "{{ env.OMNI_API_KEY }}"
  translation:
    tags:
      layer: bi
  defs_state:
    management_type: LOCAL_FILESYSTEM
